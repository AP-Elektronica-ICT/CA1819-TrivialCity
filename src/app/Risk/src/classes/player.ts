import leaflet from 'leaflet';
import { Team } from '../classes/team';

export class Player{

    constructor(playerId: number, teamId: number, userName: string, troopCount: number, reservesCount: number, playerLocation: leaflet.LatLng){
        
    }

}